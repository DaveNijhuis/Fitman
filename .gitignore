# Environment — never commit real credentials
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Test artefacts — regenerated by every pytest run
.coverage
.coverage.*
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Playwright output — regenerated by every E2E run
e2e/playwright-report/
e2e/test-results/

# Node
node_modules/
dist/
.next/
.vite/

# Database — lives in a Docker volume, not the repo
*.db
*.sqlite

# Backups — stored locally, not in the repo
backups/

# OS
.DS_Store
Thumbs.db

# Smart scale — raw BLE packet captures and calibration data contain
# personal biometric data and should never be committed
scripts/scale_analysis/*.txt
scripts/scale_analysis/scale_experiment_results.json
scripts/*.txt
scripts/scale_experiment_results.json
